import { Toggle } from "./toggle";
import { Wrapper } from "./wrapper";

export const Sidebar = () => {
    return (
        // TODO: fetch followed User & fetch recommended uesr
        <Wrapper>
            <Toggle />
        </Wrapper>
    );
};